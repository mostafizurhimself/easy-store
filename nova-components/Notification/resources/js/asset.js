// Nova Asset JS
Nova.booting((Vue, router, store) => {
    Vue.component('notification', require('./components/Notification'));
    Vue.component('notification-panel', require('./components/NotificationPanel'));
  })

before_script:
  - echo "Before script"
  - cd /var/www/html/easy-store
building:
  stage: build
  script:
    - echo "Build Stage"
    - git reset --hard
    - git pull
    - php artisan cache:clear
    - php artisan config:clear
    # - composer install
